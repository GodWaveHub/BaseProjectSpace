- エリア管理システムの概要
  - システム構成
    - GISシステムである
    - Webシステムである
  - フロントエンド
    - React, Redux, Saga
    - Okta認証
    - サポートブラウザ：Chrome, Edge
    - SPA（React/Redux）
  - バックエンド
    - Tomcat9
    - サーブレットでデータの取得・保存リクエストに対応
    - サーブレットで外部向けAPIを構築
  - データベース
    - Azure Postgres14
  - インフラ
    - 動作環境：Azure Open Shift
  - 外部連携
    - 背景地図：Zenrin Maps API
  - 運用
    - スケジューラ：Supercronic
    - ログ：標準出力に出力、ログアナリティクスで管理

- 相談事項
  - 背景
    - 他のWebシステムとエリア管理を「画面遷移」による連携で接続したい
    - 呼び出し元システム・エリア管理はいずれも Okta 認証済みであること
  - 実現したいこと（要件）
    - ①未起動時の起動：エリア管理が未起動の場合、新しいブラウザタブで起動する
    - ②状態遷移：起動後、特定のデータが選択された状態で、特定の機能を実行した状態まで自動的に遷移する
    - ③既存タブの再利用：エリア管理が起動済みの場合は、既存画面（SPAの状態を維持したまま）に対して特定機能を実行する
    - ④シングルインスタンス：起動しているエリア管理は常に１画面のみとする
  - 依頼事項
    - 上記要件を満たすための実現方法を、本ファイルの「実現方式」項目に記載してほしい
  - 確認事項（Q&A）
    - Q1. 呼び出し元システムとエリア管理は別オリジン（別ドメイン）か？
      - A1. 別オリジン（別ドメイン・別Webシステムとして稼働）
    - Q2. 呼び出し元に許容できる実装レベルは？
      - A2. JS数行までOK（`window.open()` 程度のスクリプトは記述可能）
    - Q3. 呼び出し時に渡す情報は？
      - A3. データID＋機能IDのみ
    - Q4. 既存タブで機能実行する際、ユーザーがエリア管理で編集作業中だった場合の扱いは？
      - A4. 遷移を拒否する（編集中は連携要求を受け付けず、通知のみ行う）
    - Q5. ユーザーがブックマーク等から手動でエリア管理を複数タブ開いた場合も「常に1画面」を強制するか？
      - A5. 強制する（後発タブは自動的に閉じる／使用不可にする）
    - Q6. エリア管理から呼び出し元へ結果を返す（双方向連携）必要はあるか？
      - A6. 不要（片方向のみ）

- その他備考
  - 呼び出し元の実装は最小限にしたい

- 実現方式
  - 前提整理（確認事項Q&Aより）
    - 呼び出し元とエリア管理は別オリジン（別Webシステム）
    - 呼び出し元は JS 数行まで実装可能（`window.open()` はOK）
    - 連携パラメータはデータID＋機能IDのみ
    - エリア管理で編集作業中の場合、連携要求は拒否し通知のみ行う
    - 手動起動を含め、エリア管理は常に1画面を強制する（後発タブは自動クローズ）
    - 双方向連携は不要（呼び出し元→エリア管理の片方向のみ）

  - 推奨方式：ディープリンクURL ＋ エリア管理側シングルインスタンス制御（Web Locks ＋ BroadcastChannel）
    - 全体像
      - 呼び出し元：ディープリンクURLを名前付きウィンドウで開く1行のみ
        - `window.open('https://area-mgmt.example.com/launch?featureId=xxx&dataId=yyy', 'AREA_MGMT')`
      - エリア管理側：`/launch` エントリポイント＋タブ一意性制御＋Redux/Saga による状態遷移を実装
      - 変更はほぼエリア管理側に閉じるため、呼び出し元が増えても URL 仕様の共有のみで対応可能

    - 構成要素1：ディープリンクエントリポイント（要件①②）
      - エリア管理の SPA に `/launch` ルートを追加し、クエリパラメータ `featureId` / `dataId` を受け取る
      - 起動時に Saga がパラメータを解析 → データ取得 API 呼び出し → 対象データ選択 → 機能起動のアクションを順次 dispatch し、目的の状態まで自動遷移する
      - 未起動の場合は新タブでこの URL が開かれ、そのまま要件①②を満たす

    - 構成要素2：シングルインスタンス制御（要件③④、Q5対応）
      - 各タブは起動時に `navigator.locks.request('area-mgmt-primary', ...)` でロック取得を試行する
      - ロック取得成功（＝最初の1画面）：プライマリタブとして通常動作
      - ロック取得失敗（＝既にエリア管理が起動済み）：
        1. 起動パラメータ（featureId/dataId）があれば `BroadcastChannel('area-mgmt')` でプライマリタブへ送信
        2. 自タブはガイダンス（「エリア管理は既に起動しています」）を表示して自動クローズを試行
           - スクリプト起動タブ（`window.open` 経由）は `window.close()` で閉じられる
           - ブックマーク等の手動起動タブは `window.close()` が効かない場合があるため、操作不可のブロック画面（既存タブへの誘導表示）に固定して実質的に1画面を強制する（Q5）
      - Web Locks はタブクローズ・クラッシュ時に自動解放されるため、localStorage フラグ方式のような残留状態の問題がない
      - Chrome/Edge とも Web Locks・BroadcastChannel 対応済み（サポートブラウザ要件を満たす）

    - 構成要素3：既存タブでの機能実行（要件③、Q4対応）
      - プライマリタブは BroadcastChannel で受信した featureId/dataId を検証し、リロードなしで Redux アクションを dispatch → SPA の状態を維持したまま「データ選択＋機能実行」状態へ遷移する
      - 編集作業中（Redux 上に未保存の編集状態がある）場合は遷移せず、「連携要求を受け付けられません（編集中）」のトースト通知のみ表示して要求を破棄する（Q4）

    - 構成要素4：名前付きウィンドウによるフォーカス補助
      - `window.open` の第2引数にウィンドウ名 `'AREA_MGMT'` を指定することで、同一呼び出し元からの再呼び出し時にブラウザが既存タブを再利用し、フォーカスが移りやすくなる
      - ただし既存タブへのフォーカス移動はブラウザ制約上スクリプトから完全には保証できない点は制約事項として合意しておく

  - 処理フロー（シーケンス）
    1. 呼び出し元：`window.open(deepLinkURL, 'AREA_MGMT')` を実行
    2. 新タブで `/launch` が開く（Okta 未認証ならログイン後に復帰）
    3. 新タブ：Web Locks でプライマリ判定
       - プライマリ → パラメータ解析し目的状態へ自動遷移（完了）
       - 非プライマリ → パラメータを BroadcastChannel 送信 → 自タブを閉じる
    4. プライマリタブ：受信パラメータを検証
       - 編集中でなければ Redux/Saga で目的状態へ遷移
       - 編集中なら通知のみ表示し要求を破棄

  - 認証（Okta）の考慮
    - 双方が同一 Okta テナントであれば SSO によりリダイレクト時に自動ログインされる（トークン受け渡しは不要）
    - 認証リダイレクトでディープリンクパラメータが失われないよう、`/launch` の URL を originalUri（okta-react 相当）に保存し、コールバック後に復元する

  - セキュリティの考慮
    - `featureId` / `dataId` はフロント・サーブレット双方で妥当性検証し、不正値は既定画面へフォールバック（オープンリダイレクト・XSS対策としてパラメータを DOM や遷移先 URL にそのまま埋め込まない）
    - `dataId` に対する参照権限はサーバー側で認可チェックする（URL を知っているだけではアクセスできないこと）
    - BroadcastChannel は同一オリジン限定だが、受信メッセージも同様に検証する

  - 検討した代替案と不採用理由
    - 名前付きウィンドウ単独：再利用時にフルリロードとなり SPA 状態が失われる。手動起動タブを制御できず要件④（Q5の強制）を満たせない
    - postMessage による直接連携：呼び出し元にウィンドウ参照の保持・生存確認等の実装が必要で「実装最小限」に反する。双方向連携が不要（Q6）のため過剰
    - Service Worker / SharedWorker によるタブ管理：実現可能だが Web Locks ＋ BroadcastChannel の方が実装が単純で十分
    - URLフラグメント方式（名前付きウィンドウ＋fragment navigation＋`hashchange`）：既存タブの URL とフラグメント以外（パス・クエリ）が一致する場合に限り、リロードなしで既存タブへ直接通知でき、新タブのちらつきを解消できる。ただし以下の理由により BroadcastChannel の代替にはならず、**実装は「推奨方式＋フラグメント最適化」という追加**になるため、基本形としては採用しない
      - 手動起動タブ（ブックマーク等）は呼び出し元と browsing context group が異なり、`window.name` を名乗らせても別オリジンの呼び出し元からの名前解決で発見される保証がない（Chromium では原則発見されない）。この場合は新タブが開くため、プライマリタブへの転送経路として BroadcastChannel が結局必須となる
      - 名前解決に成功してもパス・クエリが不一致（BrowserRouter で URL が変化している等）だと既存タブがフルリロードされ、SPA 状態が破壊される（BroadcastChannel 転送より悪い結果になり得る）
      - COOP（Cross-Origin-Opener-Policy）や認証リダイレクトで browsing context group が切断されると、スクリプトで開いたタブに対する名前解決も壊れ得る
      - フラグメント直接通知が機能するのは「同一呼び出し元からの再呼び出し × COOP なし × パス一致」の限定ケースのみで、ベストエフォートの最適化にとどまる
      - **結論：推奨方式（ディープリンク＋Web Locks＋BroadcastChannel）を基本形とすることを推奨する。** 新タブのちらつき解消を追求する場合にのみ、追加の最適化としてフラグメント通知の併用を検討する

  - 要件充足まとめ

    | 要件 | 実現手段 |
    |---|---|
    | ①未起動時の新タブ起動 | ディープリンクURL `/launch` ＋ `window.open` |
    | ②特定データ選択＋機能実行状態への遷移 | Saga によるパラメータ解析→データ取得→アクション dispatch |
    | ③既存タブの再利用（SPA状態維持） | BroadcastChannel でプライマリタブへ転送、リロードなしで遷移 |
    | ④常に1画面（手動起動含む） | Web Locks によるプライマリ判定＋後発タブの自動クローズ／ブロック画面 |
    | 編集中の扱い（Q4） | 遷移拒否＋通知のみ |
    | 呼び出し元の実装最小限 | `window.open` 1行のみ |

- 実装詳細補足（技術補足・注意点・見積上の考慮事項）
  - 補足1：SPA への `/launch` ルート追加
    - 実装方法
      - React Router に `/launch` ルートを追加し、連携起動専用のコンポーネント（例：LaunchPage）を割り当てる
        - 例：`<Route path="/launch" element={<LaunchPage />} />`
      - LaunchPage で `useSearchParams`（または `URLSearchParams`）により `featureId` / `dataId` を解析し、起動アクションを dispatch する
      - 起動シーケンスは Saga に集約する（パラメータ検証 → データ取得 API 呼び出し → データ選択アクション → 機能起動アクション を順次実行）
      - シーケンス開始後は `history.replace` で通常画面の URL に置き換え、`/launch` を履歴に残さない（リロード・ブックマークによる意図しない再実行の防止）
    - 技術的な補足・注意点
      - Okta 認証リダイレクト：未認証で `/launch` にアクセスした場合、クエリパラメータ込みの URL を originalUri に保存し、認証コールバック後に復元する実装が必要（パラメータ消失が起きやすい箇所のため要注意）
      - サーバー側ルーティング設定：BrowserRouter（history API）採用時は、`/launch` への直接アクセスで index.html を返す設定（Tomcat の RewriteValve / web.xml 等）が必要。HashRouter 採用時は不要
      - SPA 初期化との順序制御：アプリ起動時の初期化処理（認証確立・マスタ取得等）の完了を待ってから遷移シーケンスを開始する制御が必要（初期化前に dispatch すると失敗する）
      - パラメータ検証：`featureId` は定義済み機能IDの許可リストと照合、`dataId` はサーバー側で存在・認可チェック。不正時は既定画面へフォールバックし理由を通知する
      - 異常系 UX の設計：データ不存在・権限なし・機能利用不可など、ケースごとの表示仕様（エラーメッセージ・フォールバック先）を設計する必要がある
      - 起動シーケンス中はローディング表示を行い、ユーザーの二重操作を防止する
    - 見積上の前提条件・特記事項
      - 連携対象の機能数を前提条件として明記する（機能IDごとに「目的状態への遷移シーケンス」の個別実装・試験が必要であり、対象機能数に比例して工数が増加する）
      - 既存機能が「外部からのアクション dispatch で起動できる構造」になっていない場合、既存コードのリファクタリングが別途必要になる可能性がある（現行実装の調査工程を見積に含める／再見積条件とする）
      - 連携 URL 仕様書（パラメータ仕様）の作成と呼び出し元システムとの合意プロセスを成果物・作業範囲として明記する
      - Okta 側の設定変更（リダイレクト URI 追加等）が発生する場合の作業分担（顧客側/開発側）を明確化する
      - Tomcat / OpenShift のルーティング・デプロイ設定変更を作業範囲に含めるか否かを明記する
  - 補足2：`navigator.locks.request`（Web Locks API）によるシングルインスタンス制御
    - 実装方法
      - アプリ起動直後（初期化処理の早い段階）でロック取得を試行する

        ```js
        navigator.locks.request('area-mgmt-primary', { ifAvailable: true }, (lock) => {
          if (lock) {
            // プライマリタブ：解決しない Promise を返し、タブが閉じるまでロックを保持
            markAsPrimary();
            return new Promise(() => {});
          }
          // 非プライマリタブ：パラメータ転送 → クローズ試行 → 不可ならブロック画面
          forwardParamsViaBroadcastChannel();
          window.close();
          showBlockedScreen();
        });
        ```

      - `ifAvailable: true` により、ロックが取得できない場合は待機せず即時に `lock = null` で非プライマリと判定できる
      - `navigator.locks` の存在チェック（feature detection）を行い、未対応環境ではガイダンス表示等のフォールバックを用意する
    - 技術的な補足・注意点
      - セキュアコンテキスト必須：Web Locks API は HTTPS（または localhost）でのみ利用可能。開発・検証環境も HTTPS 前提とする
      - 制御範囲の限界：ロックが共有されるのは「同一ブラウザ × 同一プロファイル × 同一オリジン」内のみ
        - 別ブラウザの併用（Chrome と Edge）、別プロファイル、シークレット/InPrivate ウィンドウとの間では「1画面制御」は効かない
      - プライマリタブがフリーズ（ハング）した場合、ロックは解放されず新規タブがプライマリになれない。復旧手段は「フリーズしたタブを手動で閉じる」運用となる
      - タブのクローズ・クラッシュ時はブラウザがロックを自動解放するため、localStorage フラグ方式のような「フラグ残留で永久に起動不可」という問題は発生しない（本方式採用の主目的）
      - ロック保持ページは BFCache（戻る/進むキャッシュ）の対象外となる場合があるが、常駐型 SPA のため実害はない想定
      - `window.close()` が確実に効くのはスクリプトで開いたタブ（`window.open` 経由）のみ。手動起動タブはブロック画面表示が仕様上の限界となる
      - BroadcastChannel で受信したメッセージも改ざん・不正値を想定し、`featureId` / `dataId` をプライマリ側で再検証する
    - 見積上の前提条件・特記事項
      - 動作保証ブラウザ・バージョンを明記する（Chrome / Edge の最新安定版。Web Locks API は Chrome 69+ / Edge 79+ 対応）
      - 全環境（開発・検証・本番）が HTTPS で提供されることを前提条件とする
      - 「常に1画面」の強制が及ぶ範囲は同一ブラウザ・同一プロファイル内に限定される旨を制約事項として顧客と合意する（別ブラウザ併用・シークレットウィンドウ等は対象外）
      - 手動起動タブは自動クローズできない場合があり、ブロック画面表示による代替となることを仕様として合意する
      - プライマリタブがフリーズした場合の復旧手順（該当タブを手動で閉じる）を運用手順書に記載する前提とする
      - 試験工数：マルチタブ起動・タブクラッシュ・認証リダイレクトとの組み合わせ等、試験シナリオが多岐にわたるため、結合試験・システム試験の工数を通常機能より厚めに確保する